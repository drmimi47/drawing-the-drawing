GRADIA DRAW — Project Information Document
================================================================================
Purpose: Onboarding reference for collaborators and LLM coding agents. Describes
the project's intent, technical architecture, and complete current feature set.
Last updated: 2026-06-03 (multi-board design options; per-tool contextual bar;
              ribbon Color/Pen removed; per-board layer memory + global undo)
================================================================================
 

WHAT THIS PROJECT IS
--------------------
Gradia Draw is a web-based sketch-to-plan ideation tool for architects and
urban designers. It is NOT a diffusion-based image renderer. It is a precision
vector drawing environment where the designer retains fine-grained authorial
control over every line segment, and an LLM is used as a downstream co-resolver —
not a first-pass guesser.

The core workflow is linear but non-modal:

  IMPORT → SKETCH → NORMALIZE → PIN → GENERATE (Phase 2, deferred)

At each stage the designer can return to any prior stage without destructive loss.
The graph is always editable; generation emits vectors back into the same graph.

The tool is designed to outperform raw AI image generation for architectural
ideation because the designer spatially encodes programmatic intent through Pins
before generation runs. Without this spatial metadata the LLM has no finer
resolution of intent than a generic text prompt — the Pins are what make the
output controllable and site-specific.


TECH STACK
----------
- TypeScript 5, React 18, Vite 5
- Zustand 5 — global state store (graph + tool state + transient UI flags)
- React Three Fiber (R3F) + Three.js 0.169 — all drawing is WebGL, orthographic
  camera (1 world unit = 1 px at zoom 1)
- Custom GLSL shaders: metaball multi-channel concentration overlay,
  marching-ants selection outline
- @react-three/drei — Html utility for world-anchored DOM labels
- Mapbox GL JS 3 + @mapbox/mapbox-gl-draw + mapbox-gl-draw-snap-mode —
  geospatial overlay (lazy-loaded; never in main bundle)
- @turf/turf 7 — real-world area/length/coordinate calculations
- pdfjs-dist 4 — PDF first-page rasterization (dynamic import)
- jsPDF — vector PDF export (dynamic import)
- lucide-react — icons
- Deployment: Vercel (Phase 2 adds a serverless Edge Function for LLM proxying)


DATA MODEL (Source of Truth)
----------------------------
All geometry is a Planar Straight-Line Graph (PSLG). Renders are a derived cache
— never the source of truth.

  RawSample    { x, y, pressure: 0, t }              pointer stream (pressure ignored)
  Vertex       { id, x, y }                           shared at stroke intersections
  PathPoint    { v: vertexId, w: number }             per-stroke half-width reference
  Stroke       { id, color, path: PathPoint[],
                 raw?: RawSample[], straight?: bool } ordered path through vertices
  Edge         { id, strokeId, v0, v1 }               derived from strokes
  Graph        { vertices: Record<id,Vertex>,
                 strokes: Stroke[] }                  single source of truth
  LockPolygon  { id, points: {x,y}[], featherRadius } lock region (rectangle)
  IntentPin    { id, x, y, radius, intentType }       spatial prompt pin
  TextLabel    { id, x, y, text, color, size }        free-floating annotation
  Underlay     { src, opacity, pixelWidth, pixelHeight,
                 x, y, width, height }                raster import with own transform
  CanvasEntry  { id, origin: {x,y} }                  a board's identity + world offset
  CanvasDoc    { graph, lockPolygons, intentPins,     ONE board's full document:
                 textLabels, scribbles, boundary,      geometry + per-board layer
                 circulationPaths, underlay,           pipeline state (activeLayer,
                 pageWidth, pageHeight, sheetName,      maxLayerReached, context, grid,
                 activeLayer, maxLayerReached,          infill/corridor settings, map
                 context, gridType, gridSpacing, … }   state). Active board = live
                                                       store fields; inactive boards
                                                       held in inactiveCanvasDocs.
  HistoryEntry { activeDoc: CanvasDoc, activeCanvasId, undo/redo unit — the WHOLE
                 canvases, inactiveCanvasDocs }         multi-board world (global stack)
  SnapGuide    { type: 'endpoint'|'perpendicular'|    transient CAD snap guide
                 'parallel'|'extension'|'edge',        (polyline Smart Track);
                 fromPoint: [x,y], toPoint: [x,y],     lives in store.activeSnapGuide,
                 sourceEdgeId?, sourceVertexId? }      NOT part of undo history

World coordinate system: origin (0,0) at the CENTRAL board's page center, y-axis up,
right-handed. Additional boards sit at world-space offsets; their geometry is stored
in absolute world coords near their own origin (see MULTI-BOARD).
Export flips y: fx(x) = x + W/2, fy(y) = H/2 - y (top-left origin, y down).
(Export currently serializes only the ACTIVE board.)

The page sheet is an INDEPENDENT world frame: its size (pageWidth/pageHeight,
default 1080×720) is NEVER altered by imports. setPageSize() controls it
explicitly (re-fits any active underlay). An imported asset becomes an Underlay
carrying its own placement transform (centered, aspect-fit to 92% of the page
frame), so assets can be swapped without disturbing the coordinate system.

A real-world scale (metersPerWorldUnit) is held in the store: null until
calibrated by the active Mapbox projection, then updated live on every map
move/zoom via Web Mercator ground resolution math.


RENDERING PIPELINE
------------------
Three tiers, rebuilt from the graph on demand:

  Tier 1 — Raw samples:  stored on Stroke for future re-analysis
  Tier 2 — Graph:        Vertex + PathPoint source of truth
  Tier 3 — Mesh:         centripetal Catmull-Rom resampled centerline →
                         uniform-width triangle ribbon (averaged-tangent normals,
                         round end-caps, 16-step half-disk fans)

Strokes are UNIFORM thickness (pressure sensitivity deliberately removed).
Polylines use buildPolylineRibbon — sharp miter/bevel corners, no spline.
An invisible viewport-following InteractionPlane (useFrame resizes it each tick)
receives all raycasts, giving world-space pointer coords that survive pan/zoom.

RibbonMesh reuses one BufferGeometry instance as its data updates (live draw /
polyline preview / vertex drag). Three.js caches a geometry's boundingSphere on
first cull and never refreshes it, so a continuously-updated mesh kept a stale
sphere and was wrongly frustum-culled when zoomed in (small frustum) — the preview
vanished mid-draw until commit. Fix: RibbonMesh recomputes the bounding sphere in a
useLayoutEffect on every geometry change, so culling stays correct (and stays ON
for the many static committed strokes) without disabling it scene-wide.

Lineweights & line styles (drafting hierarchy, improvements C3): styling lives on
the Stroke (the persistent drawable), NOT on derived Edges — deriveEdges()
regenerates edges every call, so they cannot retain metadata. A Stroke carries
optional strokeWidth (world units; falls back to 2× path half-width) and lineStyle
('solid' | 'dashed' | 'long-dash' | 'dotted'; absent ⇒ solid). The active pen
(store.baseWidth + store.lineStyle, set via the toolbar Pen dropdown — LINEWEIGHTS
presets Fine/Standard/Bold/Thick + the four styles) is stamped onto each new stroke
at commit. buildStrokeGeometry() routes solid strokes straight through the smooth
or polyline ribbon builder; for dashed/dotted it walks the centerline by cumulative
arc length (dashArray() returns [on, off] in WORLD units, proportional to width so
the pattern stays balanced at any weight and zoom), interpolating a boundary point
at each dash edge, builds one ribbon per "on" run, and merges them into one mesh.
The SAME dashArray() drives the SVG (stroke-dasharray), PNG (ctx.setLineDash) and
PDF (setLineDashPattern) exporters, so screen and file output match. Style fields
are preserved through erase and lasso/lock re-segmentation so they survive editing.


SPATIAL INDEX & SNAPPING ENGINE
---------------------------------
src/geometry/spatialIndex.ts — SpatialGrid: a uniform spatial hash grid (64-unit
cells). Two snap target types are indexed:

  VERTEX   — every graph vertex. Because the PSLG inserts a shared vertex at every
             stroke crossing, this automatically covers intersections, endpoints,
             and any manually placed node — no separate intersection index needed.
  MIDPOINT — the midpoint of every derived edge (one per consecutive PathPoint pair
             in each stroke). Computed as (v0 + v1) / 2 at index time.

Storage: the grid stores topology + position (not pre-baked SnapPoints), so it
can be edited incrementally. String-keyed Maps key on "cx:cy" = floor(coord/cell):
  cellVerts   cellKey -> Set<vertexId>   verts whose position lands in the cell
  cellEdges   cellKey -> Set<edgeKey>    edges whose SEGMENT passes through the cell
  vertexPos   vertexId -> {x,y}          live positions
  vertexCell  vertexId -> cellKey        reverse lookup (a vertex is in one cell)
  edges       edgeKey  -> [v0,v1]        endpoint ids
  edgeCells   edgeKey  -> Set<cellKey>   reverse lookup (every cell an edge occupies)
  vertexEdges vertexId -> Set<edgeKey>   adjacency (moved vertex -> its edges)

SEGMENT-CELL INTERSECTION INDEXING (Cluster 1, long-edge blind-spot fix): edges
are bucketed into EVERY cell their segment traverses, not just the cell holding
their midpoint. Previously a long wall whose endpoints sat many cells from its
midpoint was invisible to the 3×3 nearbyEdgeEndpoints() query everywhere except
near its center, so edge snapping died along most of its length. Cell coverage is
computed by a supercover voxel traversal (Amanatides & Woo) in edgeCellKeys(),
walking cell-boundary crossings in parameter order so no cell is skipped even on
long near-axis lines; iteration is bounded by the Manhattan cell distance between
endpoints. refreshEdgeCells() recomputes an edge's cell set and re-buckets only
the diff (cells left vs. entered), keeping the drag hot path cheap. The midpoint
lies ON the segment, so its cell is always among the traversed cells — nearest()'s
MIDPOINT snap is unaffected (it distance-tests the true midpoint and de-dupes
edges that appear in several cells of the query block).
Query scans a (2R+1)² block of adjacent cells (minCx..maxCx × minCy..maxCy),
touching only the cells that can possibly hold a point within radius R — O(1)-ish
vs. O(n) full scan. For typical SNAP_THRESHOLD_PX/zoom ≈ 8–40 world units and
cell=64, the query block is 1–4 cells in each axis. SnapPoints are materialized
lazily, only for the winning candidate, so per-query allocation is ~zero.

Lifecycle (incremental, improvements C2): the grid is no longer wiped and rebuilt
on every mutation. The drag hot path (setVertexPositions) calls grid.moveVertex
per moved vertex, which re-buckets just that vertex and the midpoints of its
incident edges (O(incident) cells) IN PLACE — so dragging one vertex among
thousands stays 60 FPS. Topology changes still rebuild from scratch, routed
through one seam: setVertexPositions preserves the graph.strokes array reference,
while every topology-changing action (stroke commit, polyline finish, erase,
lasso/lock segmentation, undo, redo, clear) produces a NEW strokes array. The
Zustand subscribe() therefore rebuilds only when state.graph.strokes !==
prev.graph.strokes — an O(1) test that skips the rebuild for position-only moves
(already indexed incrementally) yet auto-covers any edge-set change with zero
per-action boilerplate. Incremental methods: addVertex / moveVertex /
removeVertex / addEdge / removeEdge; empty buckets and dead adjacency sets are
pruned on removal so no stale ids linger.

nearest(x, y, radius, exclude?) query:
  - Scans overlapping grid cells, maintains two separate best candidates:
    bestVertex (closest VERTEX within r²) and bestMid (closest MIDPOINT within r²).
  - Returns bestVertex if found, otherwise bestMid — VERTEX always takes priority,
    even when a midpoint is geometrically closer (endpoint snap is the stronger
    intent; mirrors AutoCAD/Revit snap priority).
  - exclude: Set<vertexId> — skips any target whose vid (VERTEX) or either edge
    endpoint (MIDPOINT) is in the set. Used by Vector Edit so a dragged vertex
    cannot snap to itself or to midpoints of its own edges.

Snap threshold: SNAP_THRESHOLD_PX = 8 (screen pixels) ÷ camera.zoom → world
units. Converts the activation radius into world space so the snap zone stays
visually constant as the user zooms in or out.

nearbyEdgeEndpoints(x, y) query (added for the CAD snap pipeline):
  Returns every edge whose segment passes through the 3×3 cell block around (x, y)
  as { key, a, b } (live endpoint position refs), de-duplicated across the block.
  Thanks to segment-cell indexing this now includes long edges that merely pass
  NEAR the cursor (e.g. the far end of a wall), not just edges whose midpoint is
  local. This is the small candidate set the polyline pipeline evaluates for
  perpendicular / parallel / edge snaps — O(1) cell lookups, never a full-graph
  scan. Caller reads the refs synchronously in-frame.

CAD GEOMETRIC MATH (src/geometry/snapMath.ts):
  Pure, side-effect-free 2D world-space functions; every one guards degenerate
  (zero-length / coincident) input by returning null (or the anchor) rather than
  dividing by zero. EPS = 1e-10.
    getProjectedPointOnLine(M, A, B) — foot of perpendicular from M onto the
      INFINITE line through A,B (returns A when A≈B); powers extension snap.
    getLinesIntersection2D(A, B, C, D) — crossing of the two INFINITE lines A→B
      and C→D; null when parallel/colinear (|cross of dirs| < EPS). The point can
      lie far off both segments (apparent intersection), so the caller gates by
      cursor proximity. Powers intersection snap.
    nearestPointOnSegment(M, A, B) — closest point on the FINITE segment A–B
      (clamped to endpoints) + its distance to M; powers edge snap.
    snapOrtho(anchor, p) — locks p to the H or V axis through anchor (whichever
      displacement is larger); shared by polyline Shift-ortho and Edit Shift-drag.
    checkParallelSnap(P, M, A, B, angularThresholdRad) — is vector P→M nearly
      parallel OR anti-parallel to edge A→B? Test is |cross(d̂, v̂)| = |sin θ| ≤
      sin(threshold) (symmetric about 0° and 180°, no atan2). If so, returns M
      projected onto the parallel ray from P; else null.
    checkPerpendicularSnap(P, M, A, B, distanceThreshold) — builds the line
      through P perpendicular to A→B (90°-rotated unit dir), projects M onto it,
      and returns that foot iff |M − foot| ≤ distanceThreshold; else null.

Active tools:
  Polyline (usePolyline.ts) — the CAD snap pipeline (resolveSnap, a module-level
    pure fn that reads spatialIndex via getState(), never during render). On every
    pointer move AND every click it runs a sequential priority matrix; the first
    hit wins, overrides the raw cursor with the snapped coordinate, and writes
    store.activeSnapGuide (or clears it to null):
      P1 ENDPOINT  — a true VERTEX in range: nearest committed graph vertex (index
                     hit of kind VERTEX) OR an in-progress polyline vertex within
                     SNAP_THRESHOLD_PX/zoom; locks exactly. Guide type 'endpoint'
                     (green □), emitted even on the very first placed point.
      P2 MIDPOINT  — its own tier (Cluster 2): when no vertex was in range, an edge
                     MIDPOINT index hit within SNAP_THRESHOLD_PX/zoom owns the
                     cursor and locks to the exact edge centre. Guide type 'midpoint'
                     (green △). The index returns VERTEX-over-MIDPOINT, so a nearby
                     vertex always pre-empts the midpoint (CAD object-snap priority).
      P3 INTERSECTION — (Cluster 3) for each pair of nearby committed edges, the
                     crossing of their INFINITE lines (getLinesIntersection2D); if
                     that virtual point is within SNAP_THRESHOLD_PX/zoom of the
                     cursor, lock to it — anchoring where two walls WOULD meet even
                     before they touch. Always active; the cursor-proximity gate
                     stops far crossings hijacking the cursor. O(n²) over the tiny
                     local set. Guide type 'intersection' (green ✕).
      P4 PERPENDICULAR — over candidate edges, the segment P→M forms 90° with an
                     edge within (SNAP_THRESHOLD_PX·2.5)/zoom of its perp-line
                     through P; locks M to that foot. Closest foot wins.
      P5 PARALLEL  — over candidate edges, P→M runs within 6° of an edge; projects
                     M onto the parallel ray from P. Smallest angular deviation
                     (|sin θ|) wins.
      P6 EXTENSION — (Cluster 3) with ≥2 placed vertices, projects the cursor onto
                     the INFINITE line through the last segment (P_prev→P); if it's
                     within SNAP_THRESHOLD_PX/zoom of that line, locks on so the next
                     segment continues exactly collinear. Guide type 'extension'
                     (dashed track along the trajectory; from = P_prev for a stable
                     direction). Governs the cursor's own trajectory, so it sits
                     below perp/parallel (which express intent against OTHER edges).
      P7 TRACKING  — object-snap tracking / O-TRACK. Anchors (store.trackedPoints,
                     ≤2) are acquired by hovering a committed vertex/midpoint for
                     ACQUIRE_MS (400ms; a stable-hover timer in usePolyline keyed off
                     the won snapTarget's vid/edge). Moving away, the cursor's x locks
                     to the nearest anchor x and/or its y to the nearest anchor y
                     within SNAP_THRESHOLD_PX/zoom — two anchors ⇒ lock to their
                     (T1.x, T2.y) intersection. Guide type 'tracking'; visuals owned
                     by TrackingOverlay. Below the object snaps (aligns to remembered
                     points, not geometry under the cursor), above raw edge snap.
      P8 EDGE      — nearest point on a nearby COMMITTED edge within
                     SNAP_THRESHOLD_PX/zoom (nearestPointOnSegment). Always active
                     (no session required) so a polyline can start or continue
                     EXACTLY on an existing line — landing on the span between
                     vertices to share it instead of near-missing. The hot zone is
                     kept tight (= the endpoint radius) so floating parallel-adjacent
                     to a long wall doesn't feel magnetized. Lowest priority, so all
                     higher tiers still win. Guide type 'edge'.
    P4/P5/P6 require an active session (≥1 placed vertex; P6 needs ≥2) and are
    GUARDED so they never run in the freehand Draw path. Critically, the perp/parallel
    candidate edge set is nearbyEdgeEndpoints() PLUS every segment of the IN-PROGRESS
    polyline (and in-progress vertices are P1 candidates) — so snapping engages
    mid-draw against the line you are currently drawing, not only against
    already-committed strokes. Intersection (P3) and edge (P8) use committed edges
    only (snapping onto the in-progress rubber band would fold it back on itself).
    O-TRACK anchors (P7) are per-path: cleared on finish/cancel (reset) and on
    leaving the polyline tool (setTool); acquisition runs only while snapping is on.
    Every snap sets isAdvanced=true, so
    Shift-ortho is suppressed whenever ANY snap is live (the geometric anchor wins);
    it only applies on the open-space fallback. Per-frame moves are coalesced through one
    requestAnimationFrame; activeSnapGuide is flushed there and cleared on reset /
    tool switch. NOTE: snapping places points at EXACT existing coordinates but does
    not yet topologically MERGE them (commit still mints fresh vertex ids; only
    proper interior crossings split — see addStrokeToGraph). True shared-node /
    edge-split on coincident-or-on-edge placement is a follow-up.
  Vector Edit (useVectorEdit.ts) — probes nearest() on every pointer move while
    dragging a vertex, excluding that vertex and its own edges via the exclude set.
    Applies the snapped position to setVertexPositions so connected strokes update
    live. Holding Shift locks the drag to a horizontal/vertical axis from the grab
    origin (dragOriginRef captured on grab; snapOrtho applied) — object snap still
    wins when a target is in range, mirroring the polyline tool. Shift state is read
    live off the pointer-move event. (Vector Edit keeps the legacy blue
    SnapIndicator; Polyline's visuals are owned entirely by the Smart Track overlay
    below.)

SnapIndicator (components/Canvas/SnapIndicator.tsx):
  A lineLoop built once as a unit outline (half-extent = 1) and rescaled by
  HALF_PX (6px) / camera.zoom each frame via useFrame. Screen-constant physical
  size regardless of zoom level. Two shapes:
    SQUARE  (□) — 4-vertex lineLoop — for VERTEX / endpoint targets
    TRIANGLE (△) — 3-vertex lineLoop — for MIDPOINT targets
  renderOrder 40, depthTest false, frustumCulled false — always on top.
  Now used ONLY by the Vector Edit drag (Polyline snaps render via SnapGuideOverlay,
  so the blue square doesn't double up with the green endpoint glyph).

SnapGuideOverlay — "Smart Track" guides (components/Canvas/SnapGuideOverlay.tsx):
  Reads store.activeSnapGuide and renders professional CAD alignment cues for the
  Polyline tool; renders nothing (clears instantly) when the guide is null. Lives
  inside the R3F scene graph, so all guides track pan/zoom; everything is sized in
  screen pixels (÷ camera.zoom each frame) so it stays crisp at any zoom/altitude.
  Per guide type:
    endpoint      — hollow green square at the snapped vertex (no track line).
    midpoint      — hollow green triangle at the exact edge centre (no track line).
    intersection  — green ✕ at the (apparent) crossing of two lines (no track line).
    edge          — hollow green diamond on the existing line (no track line).
    perpendicular — bright green dashed line BOUNDED last-vertex→cursor + a right-
                    angle ∟ glyph oriented to the guide direction.
    parallel      — magenta dashed line EXTENDED far past both ends (reads as an
                    infinite alignment track) + a parallel ∥ glyph.
    extension     — green dashed track EXTENDED far past both ends along the last
                    segment's trajectory (collinear continuation guide).
  Dashes are screen-constant via a ShaderMaterial: a per-vertex lineDistance
  attribute accumulates world distance, the vertex shader multiplies by uZoom, and
  the fragment shader discards gap fragments (DASH_PX/GAP_PX = 4/4) — same trick as
  MarchingAntsLine. DashedGuideLine takes an `extend` flag (false = bounded to
  from→to for perpendicular, true = ±EXTENSION_PX beyond both ends for parallel).
  GUIDE_Z = 6, renderOrder 35–36, depthTest false, frustumCulled false.


ISOLATED METABALL INTENT FIELD SYSTEM
--------------------------------------
src/utils/metaballField.ts — All math uses the Wyvill soft-object falloff:
  potential = (1 - d²/r²)³   for d < pin.radius,  else 0

  evaluatePinField(x, y, type, pins):
    Filters to the targeted IntentType only (strict per-type isolation — a
    Density pin NEVER bleeds into the Pedestrian field). Sums Wyvill potentials.

  getIntentConcentration(x, y, pins) → { total, fields, mix }:
    Runs evaluatePinField for all 4 types. total = sum. mix = normalized ratios
    (sums to exactly 1 where total > 0 — 100% of local planner intent allocated).

  getRegionConcentration(minX, minY, maxX, maxY, pins) → IntentConcentration:
    3×3 supersample of a cell, averages absolute fields before taking ratios.
    Yields the true area-weighted composition (same as what the LLM reads per
    region). Averaging fields-then-dividing is mathematically correct;
    averaging ratios would not be.

src/components/Canvas/MetaballOverlay.tsx — GPU visualization:
  A single full-coverage plane over the pins' AABB. Fragment shader (highp float
  — required for world coords up to ~10³ and d² up to ~10⁶ which overflow mediump
  ~65504) evaluates all 4 isolated fields per fragment via the Wyvill formula,
  then blends brand colors by relative concentration: green+purple overlap →
  teal-purple mix. Alpha tracks absolute total strength. VIS_THRESHOLD = 0.15
  drops background fragments.
  Pin streaming (improvements C1): pins are no longer passed as flat uniform
  arrays (which hard-capped the scene at 64 pins). They are packed one RGBA float
  texel per pin — rgba = (x, y, radius, typeIndex) — into a THREE.DataTexture
  (RGBAFormat / FloatType, Nearest min+mag filter), TEX_WIDTH = 256 texels/row,
  height grown by row on demand. The shader is GLSL ES 3.0 (glslVersion GLSL3) so
  the field loop can use the runtime bound `uTotalPins`; it decodes texel i at
  uv = ((i%W)+0.5)/W, (floor(i/W)+0.5)/H and samples it. No fixed cap — thousands
  of pins render in one draw call. Pin mutations only rewrite the texture's
  Float32Array and flip needsUpdate (no shader recompile); the texture is grown
  grow-only and disposed on unmount so rapid mutations never leak GPU memory.

src/components/Canvas/IntentConcentrationGrid.tsx — Hover inspection overlay:
  Triggered by showIntentGrid (set when hovering the Intent Pin ribbon button).
  Discretizes the AABB into 56-unit world cells, calls getRegionConcentration per
  cell, renders cell outlines as lineSegments (world space, tracks pan/zoom) and
  % labels as drei Html DOM nodes. Displays only cells with total >= VIS_THRESHOLD.
  Each label shows short codes (D/P/S/L) + rounded percentage, colored by type,
  sorted by dominance. The data is the exact mix the Phase-2 serializer reads.

src/utils/marchingSquares.ts — Generic 16-case marching squares:
  Samples a scalar field over a regular grid, emits line segments per cell via the
  full 16-case lookup table (explicit saddle handling for cases 5 and 10), then
  stitches loose segments into closed rings by endpoint matching (quantized to
  avoid float mismatch). Generic — takes any (x, y) → number field.

src/utils/intentContours.ts — Per-type isoline extraction:
  extractIntentContours(pins) runs marchingSquares INDEPENDENTLY per IntentType
  at INTENT_ISO_LEVEL = 0.15 (= shader VIS_THRESHOLD, so rings align with the
  visible field edge). Returns per-type closed Ring[] arrays. Bounds padded by
  one cell to guarantee rings close at the border.
 
src/io/intentSerialization.ts — Multi-channel LLM payload (Phase 2 prep):
  serializeIntent(graph, pins) → { vertices, regions }
  Every PSLG vertex carries an intentMix { density, pedestrian, sqf, landuse }
  sampled at its point. Every field-boundary region (from extractIntentContours)
  carries the mix sampled at its centroid. Keys are lowercase, values normalized
  0–1. Example:
    { id: "density-region-0", type: "density", positions: [...],
      intentMix: { density: 0.60, pedestrian: 0.40, sqf: 0.0, landuse: 0.0 } }
  Pure data preparation — no network call. Phase 2 wires it to api/generate.ts.


LOCKING ARCHITECTURE
---------------------
LockPolygon is drawn by a marquee drag (LASSO_LOCK tool). Visual: flat,
semi-transparent grey (binary pointInPolygon fill — the feather gradient removed
from the visual for clarity).

lockInfluenceAt(x, y, locks) in geometry/locks.ts: interior distance field.
  0.0 at polygon boundary → 1.0 at depth ≥ featherRadius inside the polygon.
  Multiple locks combined by max (union). Drives the Normalize pipeline (vertex
  delta scaled by 1 - influence) and will gate the Generate pipeline.

Intent Pins are SPATIAL PROMPTS — they do NOT freeze geometry. They mark
programmatic intent. The two-layer system (geometric lock + programmatic intent)
is the precision advantage over prompt-only generation.


NORMALIZATION
--------------
Entry point: user selects strokes with Marquee or Lasso, right-clicks inside the
selection → "Clean up" panel with a strength slider (0–1). Normalization is
ALWAYS selection-scoped and explicitly triggered — geometry never mutates
automatically. ESC cancels and reverts via revertHistory().

Pipeline (src/geometry/mutations/):

  normalizeSelection.ts — computeNormalizeTargets(graph, strokeIds):
    For each selected stroke, resolves its PathPoints to world-space centerline
    points, calls normalizeStroke(), and maps the returned target positions back
    to vertex IDs. Produces two Maps: originals (current positions) and targets
    (fitted positions). Vertices that belong to no fitted stroke keep original =
    target (they don't move). The caller lerps original → target by strength.

  normalize.ts — normalizeStroke(points: Pt[]): Pt[] | null:
    The primitive classifier + fitter. Returns 1:1 target positions or null if
    no confident fit. Classification order (first match wins):

    OPEN STROKE → best-fit straight line
      Uses PCA on the point cloud: builds the 2×2 scatter matrix [Sxx Sxy; Sxy Syy],
      solves for the dominant eigenvector via θ = 0.5·atan2(2·Sxy, Sxx−Syy), finds
      the min/max projection extents, and distributes the n output points uniformly
      along the fitted line segment. H/V snap: if the line angle is within 8°
      (HV_SNAP_RAD = 8π/180 rad) of a cardinal axis, the line is locked exactly
      horizontal or vertical before distributing points.

    CLOSURE TEST (before fitting closed shapes)
      A stroke is treated as closed when its endpoint gap < 20% of total perimeter
      (CLOSE_GAP_FRAC = 0.2) AND it has ≥ 4 unique points.

    CIRCLE (tried before polygons, only when m ≥ 8 points)
      Algebraic least-squares via the Pratt formulation. Reduces to a 3×3 linear
      system over the moments {Sx, Sy, Sxx, Syy, Sxy, Sxz, Syz, Sz}:
        [Sxx Sxy Sx] [A]   [Sxz]
        [Sxy Syy Sy] [B] = [Syz]
        [Sx  Sy  n ] [C]   [Sz ]
      Center = (A/2, B/2), radius = √(C + cx² + cy²). Solved by Gauss-Jordan
      with partial pivoting. Accepted only if RMS radial error / R < 0.18
      (CIRCLE_RESIDUAL_MAX). Each fitted point is projected radially: angle =
      atan2(p.y − cy, p.x − cx), target = (cx + r·cos, cy + r·sin).

    CORNER DETECTION (shared by triangle and rectangle)
      detectCorners() rotates the loop to start at the point farthest from the
      centroid (most likely a salient corner), then runs an augmented RDP on the
      closed loop with ε = 5% of the bounding-box diagonal (CORNER_EPS_FRAC =
      0.05). Returns the sorted indices of dominant corners.

    TRIANGLE (3 detected corners)
      Corner positions are taken directly from the loop at the detected indices.
      Every loop point is then slid to its arc-length-proportional position along
      the triangle edges via polygonTargets(): the perimeter is parameterized 0–1,
      each point's f = cumArc/perimeter is mapped to the nearest inter-corner
      segment, and the point is linearly interpolated between the two bounding
      corner positions (corners snap exactly; intermediate points slide).

    RECTANGLE (4 detected corners)
      fitRectangle() enforces right-angle geometry:
      1. Edge orientations: average the 4 edge angles using the 4× angle trick
         (sum cos(4θ) and sin(4θ), then atan2/4) to get a modulo-90° consensus
         orientation θ and its perpendicular — avoids circular-mean wraparound.
      2. Oriented bounding box: projects all loop points onto the (ux, uy) and
         (vx, vy) axes to find umin/umax/vmin/vmax, yielding four exact corners.
      3. Each detected corner is snapped to its nearest OBB corner by comparing
         its (u,v) projection to the center umid/vmid.
      4. Degenerate check: all 4 OBB corners must be claimed by distinct detected
         corners; returns null otherwise (no fit).
      Points then slide via polygonTargets() as with triangle.

  Lock gating: strength is modulated per vertex by (1 - lockInfluenceAt(v.x, v.y,
  locks)) before application. A vertex at influence 1.0 (hard lock core) does not
  move regardless of strength slider. Feather zone vertices move proportionally.

Improvements needed / known gaps:
  - No multi-stroke fitting (each stroke is fit independently; parallel corridors,
    L-intersections, and T-junctions are not detected across strokes).
  - No ellipse fitting; near-elliptical closed strokes currently fall through to
    null (no fit).
  - Urban morphology extensions (corridor detection, convex polygon generalization,
    greenway smoothing) are designed in the plan but not yet wired to the UI.


FEATURE INVENTORY
------------------

CANVAS & NAVIGATION
- R3F orthographic scene; light grey (#eef0f3) workspace backdrop, white page
  sheet; page border line (#c0c6cf; accent blue on the active board when >1 exists)
- Pan: middle-click drag, Space+drag, Pan tool
- Zoom: scroll wheel, cursor-anchored, MIN 0.2× MAX 20×
- Undo/Redo: Ctrl/Cmd+Z / Ctrl/Cmd+Shift+Z or Y. ONE GLOBAL timeline across all
  boards: each HistoryEntry snapshots the whole world (active board's full doc +
  activeCanvasId + canvases + the other boards' docs), so board creation is undoable
  and an edit rolls back on the board it happened on. See MULTI-BOARD below.
- ESC clears active selection

MULTI-BOARD (DESIGN-OPTION CANVASES / "branches")
- The workspace holds multiple boards laid out spatially as neighboring design
  options, not isolated files. Each board is a self-contained CanvasDoc living at a
  world-space `origin` offset (see DATA MODEL); the CENTRAL board sits at (0,0), so
  the single-board behavior is unchanged.
- ACTIVE vs INACTIVE: the active board's document IS the live drawingStore fields
  (every tool edits it directly); inactive boards are snapshots in
  store.inactiveCanvasDocs, rendered READ-ONLY at their offsets by InactiveCanvases
  (full fidelity: strokes, boundary fill+outline, circulation centerlines, intent
  field + pin markers, locks, grid, underlay; text/scribbles via the shared DOM
  overlays). Click any neighbor board to activate it (DrawingLayer pointer-down
  intercept while a creation tool is live; the ghost sheet's onClick otherwise).
- SPAWNING: hover just outside an edge midpoint of the active board (N/E/S/W) → a
  "+" fades in (CanvasBranchControls). Clicking it opens a chooser:
    • Duplicate board — clone the active board's full state (translateCanvasDoc
      deep-copies and shifts all ABSOLUTE-coord geometry to the new slot).
    • Blank board — a fresh board at the Context layer (choose a substrate again).
  The neighbor spawns adjacent, gutter-spaced (CANVAS_GAP) so boards never touch;
  spawning does NOT steal focus. Boards are named "Board 01", "Board 02", …
- PER-BOARD LAYER MEMORY: each board carries its OWN pipeline state — activeLayer,
  maxLayerReached, context (substrate), grid type/spacing, boundaryInfillOpacity,
  circulationWidth, metersPerWorldUnit, map active/dim/geometry — plus all its
  geometry. Switching boards swaps it all in/out; changing one board's layers never
  affects another. The right panel shows the active board's name and only its layers.
- Coordinate rule: all geometry is ABSOLUTE world coords (so it renders at the
  board's location with no per-board transform); only page furniture (sheet/grid/
  underlay) is drawn inside a `<group position={origin}>`. Page-confined tools
  (Polyline/Scribble/Text) clamp to the page rect centered at activeOrigin(state).

UI SHELL
- Top menu bar: "Gradia Draw" brand lockup (gradient app mark + clean sans
  wordmark; professional/corporate) + File / Document placeholders
- Single-row compact tool rail (46px): Pan Scribble Text | Polyline Erase Edit |
  Marquee Lasso Lock Intent Pin [Export▼]. Color & Pen (weight/style) options moved
  OUT of the rail into the per-tool contextual bar (below). Export opens a floating
  panel (SVG/PNG/PDF); every other button is a direct tool toggle. (Import substrate
  + Generate live in the right-panel Layer Hierarchy, not the rail.)
- CONTEXTUAL TASK BAR (Photoshop-style, ContextualBar): a compact floating row that
  appears only for the Scribble and Text tools. Spawns bottom-center, draggable by a
  left gripper (position persists across tool switches, clamped to the canvas).
    • Scribble → stroke color (circular swatches + custom picker) + line-weight presets.
    • Text     → stroke color + a PSEUDO size control (placeholder; real per-label
      sizing still needs the Text tool reworked — TextLabel.size is fixed at 14).
  To add a bar for another tool: add a `toolMode === 'X' && <Control/>` branch.
- Full-viewport crosshair: 1px lines via rAF dirty-flag + translate3d (GPU-
  accelerated); hidden while map active; light-mode color rgba(80,90,105,0.28)

SKETCH TOOLS
- Scribble (DRAW): freehand, uniform width, RDP-simplified, centripetal Catmull-Rom
  ribbon, round caps/joins; color + line-weight via the contextual bar
- Polyline: click-to-place, straight quads, miter joins; Shift-ortho constraint;
  CAD object snaps with priority matrix Endpoint > Perpendicular > Parallel > Edge
  (snap > Shift-ortho); edge snap lands the cursor on any existing line, snapping
  against committed geometry AND the in-progress line being drawn; double-click or
  Enter to finish; click near start to close
- Erase: swept-capsule, splits at intersections
- Pan: dedicated mode (also Space)
- Text: click to place; drag to reposition in Vector Edit mode

NORMALIZE TOOLS
- Marquee Select: rectangle drag, selects by vertex inclusion
- Lasso Select: freehand polygon selection, segments at boundary
- Edit (Vector): direct-selection editing of the LAYER-APPROPRIATE geometry on the
  ACTIVE board (geometry.editTargets.getEditTargets picks the set): Lot Boundary
  layer → boundary ring vertices; Circulation layer → centerline vertices; other
  layers → planar-graph vertices. Shows the vertices + a blue edge wireframe, drag
  to reshape, drag text labels too. ALL anchors snap to nearby geometry while
  dragging (vertex/midpoint) so a boundary/circulation vertex can land exactly on
  the lot boundary, another centerline, or a graph vertex — the dragged anchor is
  excluded by its spatial-index id ('bnd:v<i>', 'circ:<pathId>:v<i>') so it can't
  snap to itself; Shift-ortho applies when no target is in range. One undo step per
  drag (beginHistory on grab). Live moves go through setVertexPositions /
  setBoundaryPoint / setCirculationPoint (no per-move history).
  AUTO-TRIM: leaving the Edit tool (setTool away from VECTOR) re-clips every
  circulation centerline back inside the closed lot boundary (trimCirculationToBoundary
  → clipPolylineToPolygon), so edits to a corridor OR to the boundary that pushed a
  path outside are trimmed automatically — same clipping the Polyline tool applies on
  commit. One undo step; no-op when nothing is outside.
- Lock Region: rectangle drag places semi-transparent grey lock overlay

PIN TOOLS
- Intent Pin: click → type popup → drag to set radius
  Types: Density (purple), Pedestrian flow (green), SQF priority (orange),
  Land use (blue)
- Hovering the Intent Pin button: reveals per-pin type labels AND overlays
  the region concentration grid (accurate % data, not pseudo data)

SNAP INDICATORS
- SnapIndicator: screen-constant lineLoop glyph at active snap target (Vector Edit)
  SQUARE = vertex/endpoint, TRIANGLE = midpoint
- SnapGuideOverlay "Smart Track" (Polyline): hollow green square (endpoint),
  hollow green triangle (midpoint), green ✕ (intersection), hollow green diamond
  (edge / on-line), green dashed line + ∟ glyph (perpendicular), magenta dashed
  track + ∥ glyph (parallel), green dashed track (extension); screen-constant
  dashes, clears instantly when no snap is active
- O-TRACK / TrackingOverlay (Polyline): light-gray ✛ at each acquired anchor +
  thin gray dashed alignment rays from axis-aligned anchors to the locked cursor;
  reads store.trackedPoints + activeSnapGuide('tracking'), gated on snappingEnabled
- Master snapping toggle (Cluster 4): store.snappingEnabled (default true) +
  toggleSnapping(). Both resolveSnap (Polyline) and useVectorEdit short-circuit to
  raw pointer input when off; turning off also clears activeSnapGuide so cues
  vanish instantly. Magnet button in the tool rail (accent when on, grayed with a
  diagonal slash when off); F3 (CAD OSNAP standard) or Ctrl/Cmd+G toggles it from
  anywhere, with a transient SnapToast confirmation. Shift-ortho, an explicit
  modifier, still works while snapping is off.

IMPORT / EXPORT
- Image import: raster underlay, aspect-fit centered inside page frame,
  page frame NOT resized
- PDF import: first page via pdf.js (2× supersample), same placement logic
- Remove underlay: clears asset; page frame unaffected
- Export SVG (vector), PNG (2× raster), PDF (jsPDF vector + underlay)

MAP / GEOSPATIAL
- Mapbox GL JS 3 overlay, lazy-loaded (React.lazy + Suspense)
- MapboxDraw with snap modes (vertex/edge/midpoint snapping)
- Turf.js live measurements: polygon area, line length, point coords — HUD
- Live scale calibration: mercatorMetersPerWorldUnit(lat, zoom) recomputed on
  every map move/zoom; published to store as metersPerWorldUnit; shown in HUD
  as "Scale · 1 unit ≈ X m/km"; cleared on map close
- GeoJSON published to store.mapGeometry on draw events
- VITE_MAPBOX_TOKEN from .env (never hardcoded; .env is git-ignored; public pk.
  token, scope/URL-restrict it in the Mapbox account before deploy)
- Style is hardcoded to 'mapbox://styles/mapbox/streets-v12' (MapView.tsx). To drive
  the look from Mapbox Studio, publish a custom style and swap that URL (a
  VITE_MAPBOX_STYLE env var would make it configurable without code changes).


PHASE 2 — LLM GENERATE (Deferred)
-----------------------------------
Serialization infrastructure is built and ready (intentSerialization.ts,
lockInfluenceAt, geoScale). The Generate pipeline itself:

  api/generate.ts — Vercel Edge Function proxying Anthropic API
  System prompt encodes: LOCKED (influence 1.0) = fixed anchors,
    TRANSITION (feather zone) = bend/attach, OPEN (influence 0) = free;
    + intentMix spatial matrix per vertex/region
  Returns new vector segments into the PSLG graph (never raster)
  Reviewable layer: Accept merges, Reject discards, Snapshot before merge

The serializer already produces the exact payload format Claude needs.
Phase 2 activation = wire api/generate.ts + swap the Generate tab placeholder.


COORDINATE SYSTEM (for coding agents)
---------------------------------------
- World origin (0,0) = page center; y up; 1 world unit = 1 screen pixel at zoom 1
- Page bounds: x ∈ [-W/2, W/2], y ∈ [-H/2, H/2]
- Export: fx(x) = x + W/2, fy(y) = H/2 - y
- InteractionPlane (useFrame) gives world-space e.point.x/y on every tool event
- Pan: cam.position.x -= dx / cam.zoom; cam.position.y += dy / cam.zoom
- Zoom anchor: preserve cursor world point by adjusting cam.position post-zoom
- getBoundingClientRect() called live on gl.domElement; never stale cached
- Snap radius: SNAP_THRESHOLD_PX / camera.zoom → world units
