================================================================================
STEP 1: GEOMETRIC MATH UTILITIES & SNAPPING STATE EXPANSION ***DONE !!!
Target Layer: Vector Math Utilities & Zustand Snapping State
Scope: Exclusively for Polyline Construction Guidance (DO NOT modify the Draw Tool)
================================================================================

[CONTEXT]
We are adding advanced CAD-style vector alignment guidance to the Polyline Tool 
(similar to Rhino/3ds Max Object Snaps). Before modifying the tool logic, we need 
a highly performant, deterministic 2D geometric utility library and state variables 
to handle projections, parallelism, perpendicularity, and endpoint tracking.

[OBJECTIVE]
Create a geometric utility module (if necessary) and extend the 
Zustand store to hold the active snap target metadata and guide coordinates.

[TECHNICAL SPECIFICATION]
1. Mathematical Utility Functions:
   Define the following pure functions using 2D world-space coordinates (Vector2 or [number, number]):
   - `getProjectedPointOnLine(M, A, B)`: Project cursor point M onto the infinite line passing through A and B.
   - `checkParallelSnap(P, M, A, B, angularThresholdRad)`: Given the last placed vertex P, current cursor M, and reference edge A->B, determine if vector P->M is nearly parallel to A->B. Return the snapped coordinate on the parallel vector ray if within threshold.
   - `checkPerpendicularSnap(P, M, A, B, distanceThreshold)`: Given last placed vertex P, current cursor M, and reference edge A->B, construct an implicit line from P that is perpendicular to A->B. Determine if M is within distanceThreshold of this perpendicular line, and return the exact intersection coordinate.

2. Zustand Store Additions (`store.ts`):
   - Add `activeSnapGuide: SnapGuide | null` to the global state.
   - Define the `SnapGuide` type:
     type SnapGuide = {
       type: 'endpoint' | 'perpendicular' | 'parallel' | 'extension';
       fromPoint: [number, number]; // e.g., P_last
       toPoint: [number, number];   // The snapped cursor coordinate
       sourceEdgeId?: string;       // The reference edge triggering the snap
       sourceVertexId?: string;     // The reference vertex triggering the snap
     };

[AGENT EXECUTION INSTRUCTIONS]
"You are an Advanced Graphics & Applied Mathematics Engineer. Implement the 2D geometric vector utilities required for CAD polyline snapping algorithms as specified above. Ensure everything handles edge cases gracefully (e.g., preventing division-by-zero on completely vertical or horizontal reference lines). Extend our Zustand store schema to include the `activeSnapGuide` state and its corresponding setter actions. Do not touch or modify any UI drawing or tool engine components in this step."

[VERIFICATION & ACCEPTANCE CRITERIA]
- The parallel and perpendicular projection calculations pass strict unit tests with mixed floating-point coordinates.
- State setters cleanly update `activeSnapGuide` without causing re-render cascading loops.

================================================================================
STEP 2: POLYLINE TOOL SNAPPING PIPELINE INJECTION ***DONE !!!
Target Layer: PolylineTool Component / Mouse Interaction Loop
Scope: STRICTLY Polyline Tool Event Handling (DO NOT affect the Freehand Draw Tool)
================================================================================

[CONTEXT]
We have the geometric math infrastructure and snapping state ready. Now, we must 
refactor the `PolylineTool` interaction loop. When a user is in an active polyline 
session (where at least one point has been placed), the mouse movement must pull 
candidate vertices and edges from the 64-unit spatial hash grid and evaluate them 
for Endpoint, Perpendicular, and Parallel snaps.

[OBJECTIVE]
Update the `onMouseMove` event handler inside the `PolylineTool`. Implement a multi-tiered 
priority matrix that overrides the raw cursor position with a snapped coordinate 
and Populates the `activeSnapGuide` state.

[TECHNICAL SPECIFICATION]
1. Target Selection via Spatial Hash Map:
   - Query the 64-unit spatial cell containing the current mouse position M, along with its 8 immediate neighboring cells.
   - Extract all candidate Vertices and Edges inside those cells.

2. Snapping Priority Matrix (Evaluated sequentially; highest priority wins):
   - PRIORITY 1: Endpoint Snap (Start/End of existing lines or the active polyline origin). If cursor M is within `snapRadiusPx`, lock directly to the vertex. Set guide type to 'endpoint'.
   - PRIORITY 2: Perpendicular Snap. Look at candidate edges. If a line drawn from your last placed polyline vertex P to cursor M can form a 90-degree angle with a candidate edge within threshold limits, snap M to that precise 90-degree point. Set guide type to 'perpendicular'.
   - PRIORITY 3: Parallel Snap. Look at candidate edges. If the line segment from your last placed polyline vertex P to cursor M runs parallel to a candidate edge, snap the vector trajectory. Set guide type to 'parallel'.

3. State Isolation:
   - Ensure these checks evaluate ONLY when the `PolylineTool` is active AND `activePolylineVertices.length > 0`.
   - Critical Guard: Under no circumstances should these calculations alter or execute within the freehand sketch/draw tool pathway.

[AGENT EXECUTION INSTRUCTIONS]
"You are a Senior CAD Software Architect. Refactor the `PolylineTool` event loop to evaluate CAD-style object snaps on mouse move. Query our optimized spatial hash index for local segments. Implement the priority ranking engine: Endpoint -> Perpendicular -> Parallel. Override the cursor's world-space position before updating the live preview segment. If a snap occurs, commit the visual metadata to the `activeSnapGuide` store; if no snap condition is met, clear the state to null."

[VERIFICATION & ACCEPTANCE CRITERIA]
- When hovering near a perpendicular orientation relative to an adjacent parcel boundary line, the active tracking cursor cleanly locks onto the true perpendicular vector.
- The freehand draw tool behavior remains completely unimpacted and free of snapping logic interference.

================================================================================
STEP 3: SMART TRACK VISUAL GUIDE OVERLAY RENDERING
Target Layer: Canvas Overlay Renderer (Canvas 2D / Canvas WebGL Overlay Layer)
Scope: Purely visual presentation of active guidelines and geometric cues
================================================================================

[CONTEXT]
The `PolylineTool` is now successfully calculating and setting the `activeSnapGuide` 
coordinates in the Zustand store. To complete the user experience, we must visually 
draw these guidelines on-screen in real time, mimicking the "Smart Track" line 
guides found in professional drafting applications like Rhinoceros.

[OBJECTIVE]
Build or update an overlay rendering layer that reads the `activeSnapGuide` state 
and draws colored dashed guide extensions, angular alignment indicators, and text labels.

[TECHNICAL SPECIFICATION]
1. Style and Appearance Specifications:
   - Endpoint Snap: Render a small, hollow green square at the snapped vertex.
   - Perpendicular Snap: Render a small green 90-degree angle symbol (∟) at the intersection vertex on the reference line, and a thin, bright green dashed line (`setLineDash([4, 4])`) from the last placed vertex to the cursor.
   - Parallel Snap: Render a thin, bright magenta or cyan dashed line stretching along the parallel trajectory, and a small parallel glyph (∥) near the cursor.

2. Rendering Constraints:
   - Guide coordinates are stored in real-world spatial coordinates. They must scale inversely with the camera zoom parameter so that lines retain a crisp, pixel-perfect baseline width (e.g., exactly 1px thick) regardless of map altitude.
   - Ensure the rendering overlay clears cleanly the instant `activeSnapGuide` becomes null.

[AGENT EXECUTION INSTRUCTIONS]
"You are a Frontend Interactive UX Developer specializing in CAD systems. Create or update our Canvas Overlay component to draw geometric snapping indicators based on the current `activeSnapGuide` object in the Zustand store. Apply professional color schemes (Green for Ortho/Perp, Magenta for Parallel alignment) with crisp dashed layouts. Ensure all canvas transforms respect the world-space-to-screen-space matrix so that markers stay perfectly anchored during canvas panning or zooming operations."

[VERIFICATION & ACCEPTANCE CRITERIA]
- Snapping to an easement line displays a visible, high-contrast dashed guideline with zero latency.
- Zooming in or out dynamically rescales the indicators so that the snap glyphs do not appear pixelated or distorted.

